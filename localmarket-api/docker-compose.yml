services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: localmarket
      POSTGRES_PASSWORD: localmarket
      POSTGRES_DB: localmarket
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: